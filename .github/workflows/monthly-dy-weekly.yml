# Runs weekly (Mondays at 03:00 UTC) and stores the curated monthly-paying
# monthly-dy picks in Firestore. Users visiting /monthly-dy read the cached doc.
#
# Required GitHub Secrets:
#   FIREBASE_SERVICE_ACCOUNT  — JSON string of the Firebase Admin service account key
#
# Required Firestore setup:
#   Collection: monthly-dy   Documents: latest + dated snapshots
#   Security rule: allow read; (public read — writes only from Admin SDK)

name: Generate Monthly DY Picks

on:
  schedule:
    - cron: '0 3 * * 1'
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: '22'

      - name: Install script dependencies
        working-directory: scripts
        run: npm install

      - name: Generate and store monthly-dy picks
        working-directory: scripts
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node generate-monthly-dy.mjs
